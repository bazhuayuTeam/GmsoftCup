/*得到没有数据*/

var noData={};

noData.show=function(nodata){
	if(nodata==null||noData==""){
		jAlert("请正确填取地址");
		return;
	}
	var html="<div class=\"noDataDiv\">"+
    		 "<img src=\"images/noData/noData.png\" alt=\"noData\" />"+
    		 "</div> ";
	
	$(nodata).html(html);
};